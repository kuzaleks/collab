Задание будет выглядеть как-то так:
Реализовать функцию вида void create_young(ofstream& fin, ifstream& fout),
//названия можно изменить, в принципе
которая по исходному бинарному файлу(переменная fout), определяет самого младшего
студента на каждом курсе и формирует бинарный  файл из этих студентов(переменная fin).
Структура Student должна иметь следующие поля:
char[] name, Birthday date, int cource, float mark.
//возможно, нужны ещё доп. условия.   
